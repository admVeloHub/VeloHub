# VeloHub Backend - Variáveis de Ambiente (Template)
# ===================================================
# Copie este arquivo para .env e preencha com os valores reais

# MongoDB
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority&appName=ClusterName

# OpenAI API (para chatbot inteligente)
OPENAI_API_KEY=sk-your_openai_api_key_here

# Google OAuth (já existente no VeloHub)
REACT_APP_GOOGLE_CLIENT_ID=your_google_client_id_here

# CORS Origins
CORS_ORIGIN=https://your-domain.com,http://localhost:3000,http://localhost:5000

# Porta do servidor
PORT=5000

# Ambiente
NODE_ENV=development

# Google Credentials (se necessário para Google Sheets - opcional)
# GOOGLE_CREDENTIALS={\
type\:\service_account\,\project_id\:\...\}
